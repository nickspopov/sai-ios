query GetWalk($id: String!) {
  getWalk(id: $id) {
    id
    startedAt
    finishedAt
    avgPace
    avgSpeed
    distance
    duration
    walkHistory {
      history {
        latitude
        longitude
        timestamp
      }
    }
  }
}
